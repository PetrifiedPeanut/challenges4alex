# Challenge 3 : Load the below JSON string and return a list of all names
# jsonstring =  '[{"name":"Billy", "age":25, "city":"London", "job":"being awesome"},' \
#                '{ "name":"TOM", "age":22, "city":"London", "job":"being awesome"},' \
#                '{ "name":"DOGO", "age":"24", "city":"BBOP", "job":"being being"}]'
import json
class challenge3:
    def run(self, jsonstring):
        jsonObjects = json.loads(jsonstring)
        for row in jsonObjects:

        solution = jsonstring
        return solution

